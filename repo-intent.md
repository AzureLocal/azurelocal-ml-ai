# Repo intent — azurelocal-ml-ai

**Machine learning and AI inference workloads on Azure Local — GPU VM deployment and MLOps pipelines**

## What this repo is

The README is the standard HCS repo template with its `{{placeholder}}` fields
never filled in, so beyond "Machine learning and AI inference workloads on Azure Local — GPU VM deployment and MLOps pipelines"
(the GitHub description) and its root file layout, no project-specific detail
exists in the repo yet.

## How it relates to other repos

- Part of the AzureLocal org's toolkit family — see `azurelocal.github.io` for the
  org's solution catalog; this repo's actual scope relative to its siblings isn't
  documented yet

## Status

**Documentation gap.** Fill in the real purpose, shape, and status next time this
repo is touched — right now this file only proves the repo exists and names its
one-line GitHub description.
